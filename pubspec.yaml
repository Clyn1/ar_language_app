name: ar_language_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  ar_flutter_plugin: ^0.7.0
  flutter_tts: ^3.8.2
  permission_handler: ^10.4.5

  # ✅ Firebase and Auth packages
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/3d_models/
    - assets/audio/
